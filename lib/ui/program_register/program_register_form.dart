import 'package:flutter/material.dart';

class ProgramRegisterForm extends StatelessWidget {
  const ProgramRegisterForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
